<%= form_for(@board) do |f| %>
  <% if @board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@board.errors.count, "error") %> prohibited this board from being saved:</h2>
      <ul>
        <% @board.errors.full_messages.each do |msg| %>
          <li>
            <%= msg %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :category_id %>
    <br />
    <%= f.collection_select :category_id, Category.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :title %>
    <br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :maxthreads %>
    <br />
    <%= f.text_field :maxthreads %>
  </div>
  <div class="field">
    <%= f.label :hidden %>
    <br />
    <%= f.check_box :hidden %>
  </div>
  <div class="field">
    <%= f.label :pcaptcha %>
    <br />
    <%= f.check_box :pcaptcha %>
  </div>
  <div class="field">
    <%= f.label :ccaptcha %>
    <br />
    <%= f.check_box :ccaptcha %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %> 