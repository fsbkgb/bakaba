<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Bakaba" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->
<% if params[:controller] == 'pages' %>
    <%= stylesheet_link_tag "bootstrap_and_overrides", :media => "all" %>
<% else %>
    <%= stylesheet_link_tag "photon", :title => "Photon", :rel => "stylesheet" %>
    <%= stylesheet_link_tag "futaba", :title => "Futaba", :rel => "alternate stylesheet" %>
    <%= stylesheet_link_tag "burichan", :title => "Burichan", :rel => "alternate stylesheet" %>
    <%= stylesheet_link_tag "gurochan", :title => "Gurochan", :rel => "alternate stylesheet" %>
    <%= csrf_meta_tag %>
<% end %>
  </head>
<% if params[:controller] == 'pages' %>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Bakaba</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <li><%= link_to 'Rules', rules_path %></li>
              <li><%= link_to 'Help', help_path %></li>
              <li><%= link_to 'Contact', contact_path %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span3">
          <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <li><%= image_tag("logo.png", :size => "350x350", :alt => "logo")%></li>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
        <div class="span9">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "main" %>

  </body>

<% else %>
<body>
  <%= yield %>
  <%= javascript_include_tag "application" %>
</body>
<% end %>
</html>
